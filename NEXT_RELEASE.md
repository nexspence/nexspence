### ✨ Features

- **HA**: Multi-node cluster support — Redis-backed DockerV2Auth cache, distributed locks for Cleanup and BlobMigration, `/healthz` + `/readyz` health probes, `docker-compose.ha.yml` reference deployment (2 nodes + nginx + Redis + MinIO + S3 storage)

### 🐛 Bug Fixes

_No bug fixes in this release._
