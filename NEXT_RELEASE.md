### ✨ Features


### 🐛 Bug Fixes

_No bug fixes in this release._

### 🔧 Quality / Tooling

- **Migrated to the `nexspence` GitHub organization** — all three repositories moved from the `skensell201` user to the `nexspence` org (`nexspence/nexspence-core`, `nexspence/nexspence`, `nexspence/nxs`). Container images now publish under **`ghcr.io/nexspence/*`** (the old `ghcr.io/skensell201/*` paths are frozen — pull from the new org path). Updated all repo/image references across the release + website-deploy workflows, README, in-app and site docs, Helm chart, the demo-video link, and the `nxs` install URL. GitHub redirects the old repo URLs; the website auto-redeployed from the new GHCR path.
